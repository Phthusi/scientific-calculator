let threeLetterFunctions = ['cos(','sin(','tan(','log('];
let twoLetterFunctions   = ['ln('];
let simpleSymbols = [...'0123456789.','x10'];
let operators     = ['+','mul','/','-','='];
let calcfunctions = ['del','clear','off','shift','mode','ans'];
let nav = ['left','right','up','down'];
let screenContent = document.querySelector('.top-display-screen');


function navigate(input,cursor){
    let cursorIndex = cursor.cursorIndex;
    if (input === 'left'){
        
    }else if (input === 'right'){

    }else if(input === 'up'){

    }else if(input === 'down'){

    }
}
